# ===== 构建产物 / 二进制（不入库）=====

# fpk 打包产物
*.fpk

# 构建输出目录（build.sh 产物，含 fpk 与 build.log）
dist/

# v3 线上构建：payload 组装区与上游抓取缓存（含上游源码包，不入库）
build/
cache/
payload*.tar

# cron 会话快照（Hermes 运行时生成，日期时间戳格式无扩展名）
[0-9]*_[0-9]*_[0-9a-f]*

# ===== 编辑器 / 工具 =====
.zcode/

# 日志
*.log

# AI 助手指令文件（仅本地使用，不入库）
AGENTS.md
CLAUDE.md
